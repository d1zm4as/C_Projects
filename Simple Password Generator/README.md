# Simple Password Generator

Generates a random password from a fixed character set.

## Build

From this folder:

```bash
gcc main.c -o app
```

## Run

```bash
./app
```

## Notes

The password length is controlled by `PASSWORD_LENGTH` in `main.c`.
